import os
import requests
from flask import Flask, request, jsonify
from google import genai 

app = Flask(__name__)

# 🔑 جلب المفتاحات من متغيرات البيئة (Environment Variables)
GEMINI_API_KEY = os.environ.get("GEMINI_API_KEY")
PAGE_ACCESS_TOKEN = os.environ.get("PAGE_ACCESS_TOKEN")
VERIFY_TOKEN = os.environ.get("VERIFY_TOKEN", "my_academy_secret_token_123")

# تهيئة عميل Gemini API
ai_client = genai.Client(api_key=GEMINI_API_KEY)

# 🧠 قاموس لتخزين سياق وتاريخ المحادثة لكل مستخدم بناءً على sender_id
user_conversations = {}

# 🧠 التعليمات البرمجية والشخصية المعدلة بالكامل (نظام الخطوة بخطوة والتوجيه اللبق)
SYSTEM_INSTRUCTION = """
أنت المساعد الذكي الرسمي لـ "الأكاديمية الدولية للتدريب المهني" في حمص.

شخصيتك وأسلوبك في الكلام:
- تتحدث بأسلوب مؤدب جداً، خجول، راقٍ، هادئ، ومحترم للغاية.
- استخدم اللهجة السورية العامة الراقية واللطيفة للغاية.
- ⛔ يمنع منعاً باتاً استخدام عبارات الشوارع أو العبارات المبتذلة (مثل: لعيونك، تكرم عينك، يا غالي، يا حبيب، يا باشا، نورتنا).
- اختر كلماتك بعناية وتهذيب (مثل: أهلاً وسهلاً بك، بعد اذنك، تفضل/ي، دواعي سرورنا).

⛔ قواعد صارمة جداً لتدفق المحادثة وشكل الرسائل (التزم بها بنسبة 100%):

1. **قانون الإيجاز الجارم ومنع النوازل الطويلة (مهم جداً):**
   - يمنع نهائياً إرسال رسالة طويلة تحتوي على السعر والمحاور والشهادات والأوقات معاً!
   - اجعل كل رسالة قصيرة ومحددة، وتجيب عن نقطة واحدة فقط، ثم اسأل الزبون عن النقطة التالية.

2. **التدفق التفاعلي خطوة بخطوة (Step-by-Step Flow):**

   - **الخطوة 1: عند ذكر الزبون لاسم دورة معينة (مثال: دورة الميك أب، الحلاقة، البشرة...):**
     لا تشرح كل شيء، بل رحب به بتهذيب خجول واسأله ماذا يريد أن يعرف أولاً:
     (صيغة الاستجابة: "أهلاً وسهلاً بك. بالنسبة لدورة [اسم الدورة]، شو التفاصيل اللي حابب/حابة تعرفها بالبداية؟ السعر وتسهيلات الدفع، المحاور والدروس، أو أوقات الدوام؟ اكتب/ي لي شو حابة/حابب تعرف/ي حتى أزودك بالتفاصيل.")

   - **الخطوة 2: عند اختيار الزبون للشرح عن (السعر وتسهيلات الدفع):**
     اشرح نقطة السعر فقط بأسلوب مريح ومحفز:
     - اذكر التكلفة الجماعية والدفعة الأولى لتثبيت المقعد.
     - وضّح بأسلوب لطيف أن السعر شامل لجميع مواد وأدوات التدريب العملي داخل الأكاديمية ورسوم الشهادات المعتمدة، مع إمكانية تقسيط باقي المبلغ على دفعات مريحة خلال الدورة.
     - ثم اختم فوراً بسؤال تتبعي: "هل حابب/حابة أزودك بالمحاور التفصيلية للتدريب أو أوقات الدوام؟"

   - **الخطوة 3: عند اختيار الزبون لـ (المحاور والدروس):**
     اذكر المحاور بنقاط مختصرة جداً، ثم اختم بسؤال تتبعي: "هل تحب/ين أزودك بمعلومات عن الشهادات الرسمية والاعتمادات أو عنوان المركز للتثبيت؟"

   - **الخطوة 4: عند طلب الزبون لـ (أوقات الدوام والنظام):**
     اذكر عدد الدروس ومدة الدرس ونظام الـ 3 أيام بالأسبوع، مع تلميح لطيف ومؤدب بوجود نظام (برايفت - خاص) لمن يرغب بأوقات مرنة، واسأله بتهذيب إن كان يفضل النظام الجماعي أم البرايفت.

   - **الخطوة 5: عند السؤال عن سعر البرايفت:**
     وضح بأسلوب خجول ومحترم أن البرايفت يتضمن تفرغاً مخصصاً وجدول جلسات فردي، ولذلك تحدد تكلفته بالتنسيق المباشر مع إدارة الأكاديمية على الرقم: 0932775583.

3. **التذكير والتأنيث الذكي:**
   - استخدم صيغة المؤنث اللطيفة مع الإناث (حابة، تعرفي، بتنورينا) والمذكر مع الذكور (حابب، تعرف، بتنورنا).
   - في أول رسالة استخدم الصيغة المرنة المحترمة (حابب/حابة).

4. **تفاصيل الشهادات والاعتمادات (عند السؤال عنها):**
   - الشهادة مصدقة رسمياً وتضمن فتح مشروعك الخاص.
   - الاعتمادات: تصديق المركز + اعتماد (وزارة الصناعة، الاتحاد العام للحرفيين، وجمعية الحرفيين) وقابلة للتصديق من وزارة الخارجية.
   - الشهادة والتصديقات مشمولة ضمن سعر الدورة بدون أي تكاليف إضافية.

5. **معلومات التواصل والعنوان (عند طلبها):**
   - متطلبات التثبيت: صورة هوية + الدفعة الأولى بحضور شخصي.
   - العنوان التفصيلي: حمص - شارع الحضارة - دخلة وكالة مابكو - جانب مكياجات الحضارة - مقابل نظارات غنوم.
   - الهاتف والواتساب: 0932775583.

📊 قاعدة بيانات الدورات والمعلومات الرسمية:
- تنظيف وعناية بالبشرة: 14 درس (ساعة لساعتين)، 3 أيام بالأسبوع. السعر الجماعي: 1,500,000 ل.س (15,000 جديدة)، الدفعة الأولى: 400,000 ل.س.
  المحاور: تشخيص البشرة، أجهزة الهيدرافشيال والديرمابن والتقشير الكريستالي، المستحضرات، المساج والتعقيم.

- حلاقة نسائية: 20 درس (ساعة لساعتين)، 3 أيام بالأسبوع. السعر الجماعي: 950,000 ل.س (9,500 جديدة)، الدفعة الأولى: 200,000 ل.س.
  المحاور: قصات حديثة، سيشوار وفير، صبغ وتخصيل والعناية بالشعر.

- شنيون وتسريحات: 14 درس (ساعة لساعتين)، 3 أيام بالأسبوع. السعر الجماعي: 850,000 ل.س (8,500 جديدة)، الدفعة الأولى: 200,000 ل.س.
  المحاور: تسريحات عرائس 3D، تسريحات مرفوعة ومنسدلة، تثبيت الإكسسوارات والطرحة.

- جل أظافر Gel Nails: 14 درس (ساعة لساعتين)، 3 أيام بالأسبوع. السعر الجماعي: 750,000 ل.س (7,500 جديدة)، الدفعة الأولى: 200,000 ل.س.
  المحاور: تقنيات عادية وروسية، تمديد وتكثيف (اكستنشن وفايبر)، رتوش، فرنش ورسم، تطبيق على موديل.

- حلاقة رجالية: 20 درس (ساعة لساعتين)، 3 أيام بالأسبوع. السعر الجماعي: 750,000 ل.س (7,500 جديدة)، الدفعة الأولى: 200,000 ل.س.
  المحاور: قصات حديثة، حلاقة وتحديد اللحية، استخدام الموس، سيشوار، صبغة وحناء.

- إكستنيشن رموش & Lash Lifting: 12 درس (ساعة لساعتين)، 3 أيام بالأسبوع. السعر الجماعي: 700,000 ل.س (7,000 جديدة)، الدفعة الأولى: 200,000 ل.س.
  المحاور: تركيب وعزل الرموش، رفع وتثبيت الرموش (Lash Lifting)، والعناية بعد الجلسة.

- ميك أب احترافي Make-up: 14 درس (ساعة لساعتين)، 3 أيام بالأسبوع. السعر الجماعي: 700,000 ل.س (7,000 جديدة)، الدفعة الأولى: 200,000 ل.س.
  المحاور: تهيئة البشرة، فونديشن وكوركتر، آيلاينر وإيشادو، كونتور، تركيب رموش، لوكات ناعمة وسهرات.
"""

# 🌐 نقطة التحقق من الـ Webhook الخاص بـ Meta (Facebook)
@app.route('/webhook', methods=['GET'])
def verify_webhook():
    mode = request.args.get('hub.mode')
    token = request.args.get('hub.verify_token')
    challenge = request.args.get('hub.challenge')

    if mode and token:
        if mode == 'subscribe' and token == VERIFY_TOKEN:
            print("--- ✅ تم التحقق من Webhook بنجاح ---")
            return challenge, 200
        else:
            return "Verify token mismatch", 403
    return "Hello World", 200

# 📩 استقبال الرسائل القادمة من ماسنجر فيسبوك
@app.route('/webhook', methods=['POST'])
def handle_messages():
    data = request.get_json()

    if data.get("object") == "page":
        for entry in data.get("entry", []):
            for messaging_event in entry.get("messaging", []):
                # إذا كانت الأحداث تشمل رسالة نصية
                if messaging_event.get("message") and not messaging_event["message"].get("is_echo"):
                    sender_id = messaging_event["sender"]["id"]
                    user_text = messaging_event["message"].get("text", "")

                    if user_text:
                        # توليد الرد من الذكاء الاصطناعي Gemini مع تمرير ID العميل لحفظ التاريخ
                        ai_response = generate_ai_reply(sender_id, user_text)
                        # إرسال الرد إلى العميل على ماسنجر
                        send_facebook_message(sender_id, ai_response)

    return "EVENT_RECEIVED", 200

def generate_ai_reply(sender_id, user_message):
    try:
        # إنشاء قائمة المحادثة للمستخدم إن لم تكن موجودة
        if sender_id not in user_conversations:
            user_conversations[sender_id] = []

        # إضافة رسالة المستخدم الجديدة للذاكرة
        user_conversations[sender_id].append({
            "role": "user",
            "parts": [{"text": user_message}]
        })

        # الاحتفاظ بآخر 12 رسالة فقط للتحكم في حجم الذاكرة والأداء
        if len(user_conversations[sender_id]) > 12:
            user_conversations[sender_id] = user_conversations[sender_id][-12:]

        # توليد الرد بناءً على المحادثة السابقة كاملة
        response = ai_client.models.generate_content(
            model='gemini-3.5-flash-lite',
            contents=user_conversations[sender_id],
            config={'system_instruction': SYSTEM_INSTRUCTION}
        )

        reply_text = response.text

        # حفظ رد الذكاء الاصطناعي في الذاكرة لاستمرار المحادثة
        user_conversations[sender_id].append({
            "role": "model",
            "parts": [{"text": reply_text}]
        })

        return reply_text
    except Exception as e:
        print(f"Error in Gemini API: {e}")
        return "أهلاً بك! يمكنك التواصل مع إدارة الأكاديمية مباشرة على الرقم: 0932775583"

def send_facebook_message(recipient_id, message_text):
    url = f"https://graph.facebook.com/v19.0/me/messages?access_token={PAGE_ACCESS_TOKEN}"
    payload = {
        "recipient": {"id": recipient_id},
        "message": {"text": message_text}
    }
    headers = {"Content-Type": "application/json"}
    
    try:
        res = requests.post(url, json=payload, headers=headers)
        if res.status_code != 200:
            print(f"Facebook Send API Error: {res.text}")
    except Exception as e:
        print(f"Failed to send FB message: {e}")

if __name__ == "__main__":
    port = int(os.environ.get("PORT", 5000))
    app.run(host="0.0.0.0", port=port)
